# ============================================================
# 新项目 .gitignore — 系统级分享边界（一票否决制）
#
# 设计原则：
#   1. 只定义「不分享」，不删除任何本地文件
#   2. private/ = 私有区，物理隔离，一票否决
#   3. config.yaml = 本机真实配置（含 key 与绝对路径），绝不分享
#   4. 所有 API key / 环境 / 缓存一律排除
# ============================================================

# ---------- 私有区（一票否决，绝不分享）----------
private/

# ---------- 敏感信息 ----------
config.yaml
api-key.txt
*.key
*.pem
.env
.fernet_key

# ---------- 环境 ----------
.venv/
venv/
__pycache__/
*.pyc
.DS_Store
.idea/
.vscode/

# ---------- 数据 / 产物（通常不分享，按项目需要显式加白）----------
*.duckdb
*.duckdb.wal
*.log
output/
dist/
build/
*.tar.gz

# ---------- 分享包产物（可重建）----------
share_*/
