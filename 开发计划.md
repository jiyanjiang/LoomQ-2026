# LoomQ 开发计划（截止前精修路线）

> 版本：v1 · 创建：2026-08-21 · 截止：2026-08-25 12:00 UTC+8（剩余 4 天）
> 基线：提交 `b6d9552` 已 push；官方预检 `prepare_submission.py --team-id jiyanjiang` 通过；全量自检通过（L1 套件 63/63、evaluator 6/6、pytest 42/42）。
> 原则：只做确定性最高、性价比最高的精修；每步完成即自检、暂停回报；截止前一切 push 自动进入收件。

## 0. 当前得分基线（评分总表 100+12）

| 模块 | 分值 | 预估 | 确定性 |
|---|---|---|---:|
| L1 通用中间层（45） | 入门 12 + 进阶至 35 + 真机 10 | 30–35 | 高（公开 8 电路全过 + 三平台真机证据） |
| L2 智能体（30） | 客观 20 + 交互 10 | 12–28 | 中（隐藏 prompt 变体未测） |
| L3 混合编译（15） | 确定性判定 | 15 | 高（公开 1/1 + 隐藏矩阵 11/11） |
| 工程与产品化（10） | 文档/叙事 | 8–10 | 中 |
| Bonus 自定义 RISC-V（+8） | 三件套齐 | 8 | 高（16/16 端到端） |
| Bonus 新手引导（+4） | 主观 | 3–4 | 中 |
| **合计** | **112** | **86–110** | |

## 1. 精修方向总览（按性价比排序）

### 阶段 1：L1 隐藏电路全覆盖补测（确定性最高，先做）

- **做什么**：官方隐藏电路 = GHZ-5 / QFT-4 / Grover-3 / Random×3（8 个）。现 21 用例已覆盖 QFT-4、Grover-3、GHZ-3（3-qubit 版），缺 **GHZ-5** 与 **Random×3**。
  - `tests/cases.yaml` 增加 `ghz5.qasm`（5-qubit GHZ）+ 3 个随机电路（随机门序列，`qasm_semantics.py` 尺子自动算参考分布）。
  - 跑 `run_test_suite.py` 验证 24 用例 × 3 后端 = 72 项全过。
- **为什么**：L1 进阶按"隐藏电路覆盖度线性计分"，多过 1 个就多拿一段；且能提前暴露随机电路的厂商兼容坑（如 braket 对参数门/多控门的边界情况）。
- **验收**：72/72 PASS；报告落盘 `tests/test_suite_report.json`。
- **风险**：低。纯本地、零外部依赖。

### 阶段 2：L2 隐藏 prompt 变体自测（提升客观分确定性）

- **做什么**：评测用**未公开 prompt 变体**，三类任务（生成 / 纠错修复 / 智能选后端），靠关键词匹配硬编码必挂。写 12 个变体 prompt（每类 4 个：同义改写、加约束、错误注入、多后端询问），跑 `agent_chat` 验证：
  1. 返回文本含可解析 OpenQASM 2.0；
  2. 自验闭环通过率（生成 → 三后端本地跑 → 分布匹配）；
  3. 纠错任务是否保持用户声明意图。
- **为什么**：L2 客观分当前仅按公开 GHZ PASS 估 12–18，变体自测可把下限做实、提前修薄弱点。
- **验收**：12 case 自测报告（每 case：输入/输出/自验结果/PASS 与否）。
- **风险**：需 DeepSeek API key（`config.yaml` 已有）；`deepseek-v4-flash` 与正式评测同模型。

### 阶段 3：Web 展示层收口（影响 L2 交互 10 分 + 工程叙事 + Bonus 4 分）

- **做什么**：
  1. `schwinger.js` 的 LEVELS 关卡定义从内嵌 JS 迁移到 `data/` JSON（SOP §3.6 已知缺口，内容/呈现脱耦）；
  2. 实测 web 一键启动 + 证据包声明的 3 个用户体验任务逐条走通（截图留痕）；
  3. 必答题叙事打磨："你的工具让哪一类原本进不来的人第一次能用上量子计算？"——用 Web 工作台 + 施温格积木无代码引导作佐证，文案 + 证据打包。
- **为什么**：L2 交互 10 分由工作人员以最终代码实际运行评分；工程叙事 10 分含必答题；Bonus 新手引导 4 分主观评定。
- **验收**：web 启动命令可复现；3 任务截图；README 叙事段完成。
- **风险**：低。均为展示层改动，不动 adapter 契约。

### 阶段 4：最终发布自检 + 提交（截止前 1 天）

- **做什么**：跑 SOP §5.1 四项（evaluator all / run_test_suite / check-ignore 敏感文件 / 官方文件完整性）+ §5.2 提交流程 + `prepare_submission.py` 预检 + push。
- **验收**：全部通过；`git log` 确认 HEAD 已上 origin。

## 2. 时间表（建议）

| 日期 | 事项 |
|---|---|
| 8/21（今天） | 阶段 1（L1 隐藏电路补测）——完成后暂停回报 |
| 8/22–8/23 | 阶段 2（L2 变体自测）→ 阶段 3（web 收口） |
| 8/24 | 阶段 4 最终自检 + 提交 + 预检 |
| 8/25 12:00 | 截止（之后任何 push 不再收件） |

## 3. 不做清单（明确放弃，防发散）

- 不重做门映射/算法内核（evaluator 6/6、63/63 已证明达标，再改有回归风险）。
- 不加新后端平台（三平台已统一，真机证据 2 平台已达标）。
- 不重写 L3 编译架构（隐藏矩阵 11/11 全过，属确定性满分项）。
- 不做赛后复盘（截止后另立）。

## 4. 收尾检查表（每次 push 前）

- [ ] `git status -sb` 工作区意图明确
- [ ] `git diff --cached --name-only | grep -iE "api.?key|machines|spinq.txt"` 无敏感文件
- [ ] evaluator `--level all` 6/6
- [ ] `run_test_suite.py` 全过
- [ ] `prepare_submission.py --team-id jiyanjiang` 通过
